(a, b) => {
    .. colors;

    a = {
        a: 1,
        b: 2,
        c: 3}
    b = {
        a: 1,
        b: 1,
        d: 1}
    const diff = .. diff(a, b);
    console.log(.. colors.red('asdf'));

    if (diff) return . dump(diff, a, true);}
